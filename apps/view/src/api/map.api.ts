// Variables

const backendUrl = process.env.BACKEND_URL ?? "http://localhost";
const backendPort = process.env.BACKEND_PORT ?? "8080";

// API Calls

const getMapTile = async () => {
};

const MapAPI = {
  getMapTile,
};

export default MapAPI;
